import {defineComponent} from "vue";
import {getQuote} from "@/api/requests/quote";
import {getAllOrderOverviews} from "@/api/requests/history";

export default defineComponent({
    data() {
        return {
        }
    },
})
